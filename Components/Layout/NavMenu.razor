<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">TralaAI Dashboard</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<aside class="nav-menu shadow-sm d-flex flex-column p-0"
    style="width: 230px; min-height: 100vh; position: fixed; top: 56px; left: 0;">
    <nav class="nav flex-column py-3">
        <div class="nav-item px-3 mb-2">
            <NavLink class="nav-link rounded" href="" Match="NavLinkMatch.All" activeClass="active"
                style="height: 60px;">
                <span
                    class="bi bi-house-door-fill d-flex flex-column align-items-center justify-content-center text-center"
                    aria-hidden="true"></span>
                <span>Home</span>
            </NavLink>
        </div>
        <div class="nav-item px-3 mb-2">
            <NavLink class="nav-link rounded text-center" href="ai-predictions" activeClass="active"
                style="height: 60px;">
                <span class="bi bi-list-nested d-flex flex-column align-items-center justify-content-center text-center"
                    aria-hidden="true"></span>
                <span>AI Predictions</span>
            </NavLink>
        </div>
        @* <div class="nav-item px-3 mb-2">
            <NavLink class="nav-linkmrounded text-center" href="heatmap" activeClass="active" style="height: 60px;">
                <span class="bi bi-graph-up  d-flex flex-column align-items-center justify-content-center text-center" aria-hidden="true"></span>
                <span>Heatmap</span>
            </NavLink>
        </div> *@
        <div class="nav-item px-3 mb-2">
            <NavLink class="nav-link text-center" href="history" activeClass="active" style="height: 60px;">
                <span
                    class="bi bi-clock-history d-flex flex-column align-items-center justify-content-center text-center"
                    aria-hidden="true"></span>
                <span>History</span>
            </NavLink>
        </div>
    </nav>
</aside>

<style>
    .nav-menu {
        transition: box-shadow 0.2s;
        z-index: 1040;
    }

    .nav-link {
        font-weight: 500;
        padding: 0.75rem 1rem;
        transition: background 0.15s, color 0.15s;
    }

    .nav-link .bi {
        font-size: 1.2rem;
    }
</style>
