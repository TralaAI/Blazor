<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Trala-AI Dashboard</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<aside class="nav-menu shadow-sm d-flex flex-column p-0"
    style="width: 230px; min-height: 100vh; position: fixed; top: 56px; left: 0;">
    <nav class="nav flex-column py-3">
        <div class="nav-item px-3 mb-2">
            <NavLink class="nav-link d-flex align-items-center rounded" href="" Match="NavLinkMatch.All"
                activeClass="active">
                <span class="bi bi-house-door-fill me-2" aria-hidden="true"></span>
                <span>Home</span>
            </NavLink>
        </div>
        <div class="nav-item px-3 mb-2">
            <NavLink class="nav-link d-flex align-items-center rounded" href="ai-predictions" activeClass="active">
                <span class="bi bi-list-nested me-2" aria-hidden="true"></span>
                <span>AI Predictions</span>
            </NavLink>
        </div>
        <div class="nav-item px-3 mb-2">
            <NavLink class="nav-link d-flex align-items-center rounded" href="heatmap" activeClass="active">
                <span class="bi bi-graph-up me-2" aria-hidden="true"></span>
                <span>Heatmap</span>
            </NavLink>
        </div>
        <div class="nav-item px-3 mb-2">
            <NavLink class="nav-link d-flex align-items-center rounded" href="history" activeClass="active">
                <span class="bi bi-clock-history me-2" aria-hidden="true"></span>
                <span>History</span>
            </NavLink>
        </div>
    </nav>
</aside>

<style>
    .nav-menu {
        transition: box-shadow 0.2s;
        z-index: 1040;
    }

    .nav-link {
        font-weight: 500;
        padding: 0.75rem 1rem;
        transition: background 0.15s, color 0.15s;
    }

    .nav-link.active,
    .nav-link:hover {
        /* No background or color */
    }

    .nav-link .bi {
        font-size: 1.2rem;
    }
</style>
